name: Test custom pack version
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          args: version
          version: '0.23.0'
      - name: Assert pack ran with custom version
        run: |
          echo "PASS: pack ran with custom version 0.23.0"
